/// EastmarkHK publisher identity (footer / about), kept in its own file so
/// it matches the pattern used across other EastmarkHK apps.
class EastmarkBrand {
  EastmarkBrand._();

  static const companyName = 'EastmarkHK';
  static const websiteUrl = 'https://eastmarkhk.com/';
}
